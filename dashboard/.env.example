# A-SWARM Mission Control Environment Configuration

# WebSocket URL for backend connection
VITE_ASWARM_WS_URL=wss://mission-control.example.com/ws

# Authentication token (JWT or similar)
VITE_ASWARM_TOKEN=REDACTED_JWT_OR_MACAROON

# Authentication mode: "protocol" uses WebSocket subprotocol, "query" uses query string
VITE_ASWARM_AUTH_MODE=protocol

# For local development:
# VITE_ASWARM_WS_URL=ws://localhost:8000/ws
# VITE_ASWARM_TOKEN=
# VITE_ASWARM_AUTH_MODE=protocol